/// <reference path="../../adonis-typings/index.d.ts" />
import { ActionOptions, BasePolicyContract } from '@ioc:Adonis/Addons/Bouncer';
/**
 * Every policy must extend the base policy
 */
export declare class BasePolicy implements BasePolicyContract {
    static booted: boolean;
    static actionsOptions: {
        [key: string]: ActionOptions;
    };
    /**
     * Boot the policy
     */
    static boot(): void;
    /**
     * Store action actions. This is usually invoked via a decarator
     */
    static storeActionOptions(propertyName: any, options: ActionOptions): typeof BasePolicy;
}
